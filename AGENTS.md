# AGENTS

## 项目信息

- 项目名称：青空归处
- 项目类型：小说创作
- 主要语言：中文

## 创作工作流

本项目有两种工作流，按用户给的材料区分：

### 工作流 A：大纲驱动（默认）

1. **用户写大纲** → 大纲是唯一的故事走向来源， 不得擅自改动
2. **AI 根据大纲写正文** 
3. **用户阅读正文，用 HTML 注释标注问题** 
4. **AI 按批注修改正文** → 逐条处理批注，改到用户满意为止

### 工作流 B：全文批改

1. **用户手动写完全文**
2. **AI 根据注释要求修改原文**
   - 全文中的 `{...}` 标记 = 润色指令：对 `{}` 包裹的内容进行润色，保留原意与情节，只优化表达
   - HTML 注释的处理同工作流 A 的批注规范（见下）
   - 两种标记可并存，各自按规则处理
   - `{}` 处理后去掉花括号，批注处理完即删

### 第 2 步：写正文规范（工作流 A）

- 动笔前通读对应大纲段落和已有的相邻章节，保证设定、人名、时间线连贯
- 只把大纲的故事走向展开为正文，不新增大纲没有的剧情事件
- 细节（对话、动作、场景描写）可以合理发挥，但不得与大纲或前文冲突
- 参照 novel-writing skill 的方法论（见下方索引）写作
- 大纲留白处如果无法确定走向，停下来问用户，不要自行编造关键情节
- 环境描写如有提供文档需先查设定文档
- 避免时间矛盾（如：未买帽子前不能写"帽子里的耳朵"）
- 避免场景矛盾

### 第 3-4 步：HTML 批注规范

用户用 HTML 注释在正文中标注问题，如：

- ：事实性错误（设定冲突、逻辑矛盾、时间线错误、人名错误等），必须修正
- ：需要修改的地方（表达不佳、节奏问题、描写不足等），按注释要求改写
- ：疑问或待确认项，先在回复中回答，不要直接改正文

处理规则：

- **批注是修改指令，不是正文内容**，任何情况下不得把注释文字写进正文
- 逐条处理，每条批注对应一处修改；同一处有多条批注时合并处理
- 修改只覆盖批注点名的位置及其必要的上下文，**不重写未批注的段落**
- 处理完一条批注后，删除该条注释；所有批注清零即完成一轮修改
- 若不同意某条批注，在回复中说明理由并保留该注释，等用户裁决
- 修改完成后汇报：每条批注 → 做了什么处理（或为何不处理）

### 禁止事项

- 不修改大纲文件（`.大纲.md`），除非用户明确要求
- 不删除、不"顺手优化"用户写的批注以外的内容
- 不在正文中保留任何 `<!-- -->` 注释（批注处理完即删）

## 写作规范

### 正文风格要求

- 场景：五感描写（视觉+听觉+触觉为主），避免空洞概括
- 节奏：动作链转场（推门→进店→挑选），避免"然后""接着"滥用
- 细节：装备/物价等设定具体化（如船本体6000元、长矛试手感）

### 大纲格式

- 只写故事走向，不按章节分隔
- 不写具体对话和细节
- 按时间顺序串联事件

## 工具与技能

- novel-writing skill：小说创作方法论

该skill有多个子主题，skill.md中的内容仅为索引，具体攥写时需要根据故事内容灵活查看使用子主题