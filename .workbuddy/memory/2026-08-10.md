# 2026-08-10

- 在仓库根目录创建了仿番茄小说的网页阅读器 `index.html`（单文件，CSS/JS 内联），用于 GitHub Pages 部署。
  - 章节源：`心火长明/` 下的 `N_标题.md`，fetch 时按段 encodeURIComponent；渲染前会剥离 HTML 注释（兼容批注工作流）。
  - 章节清单硬编码在 index.html 内 `BOOK.chapters` 数组，新增章节需手动追加一行。
  - 功能：仿真翻页（CSS 多列分页）/上下滚动两种模式、白天/护眼/夜间三主题、Noto Sans SC 与 Noto Serif SC 切换（Google Fonts CDN + 系统字体回退）、字号 14–28、三档行距、目录抽屉、进度与设置存 localStorage（键 `xinhuo-reader-v1`）。
  - 注意：file:// 协议无法加载章节，需经 http(s) 访问；`.大纲.md` 已被 .gitignore 排除，不会发布。
